# Set git config
git config --global user.email "${GIT_EMAIL}"
git config --global user.name "${GIT_USER}"
# Check out dotCMS.
git clone https://github.com/dotCMS/dotCMS.git ~/dotCMS
# Check out atomanti.
git clone https://github.com/lord-tyrion/atomanti.git ~/atomanti
